<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.financial.dao.BusinessDataDao">
     
       <resultMap id="businessData" type="cn.financial.model.BusinessData">
           <id column="id" property="id" jdbcType="INTEGER" />
           <result column="oId" property="oId" jdbcType="VARCHAR" />
           <result column="info" property="info"  />
           <result column="createTime" property="createTime" jdbcType="TIMESTAMP" />
           <result column="updateTime" property="updateTime" jdbcType="TIMESTAMP" />
           <result column="typeId" property="typeId" jdbcType="VARCHAR" />
           <result column="uId" property="uId" jdbcType="VARCHAR" />
           <result column="year" property="year" jdbcType="INTEGER" />
           <result column="month" property="month" jdbcType="INTEGER" /> 
           <result column="status" property="status" jdbcType="INTEGER"/> 
           <result column="delStatus" property="delStatus" jdbcType="INTEGER"/>
           <result column="sId" property="sId" jdbcType="INTEGER"/>
           
          <!--  <association property="users"  resultMap="userResultMap" ></association> -->
       </resultMap> 
          
       <!--新增损益数据 -->
       <insert id="insertStatement" parameterType="businessData" keyProperty="id">
            insert into businessData(id,oId,info,createTime,typeId,uId,year,month,status,delStatus,sId)
            values (#{id},#{oId},#{info},now(),#{typeId},#{uId},#{year},#{month},#{status},#{delStatus},#{sId})
       </insert>
      
      
       <!-- 查询所有的损益数据 -->
       <select id="getAll" resultMap="businessData">  
        select id,oId,info,createTime,updateTime,typeId,uId,year,month,status,delStatus,sId
        from  businessData 
        where delStatus=1 
        ORDER BY createTime DESC
       </select>
       
      <!--  根据id查询损益数据 -->
       <select id="selectStatementById" parameterType="String" resultMap="businessData" >
           SELECT id,oId,info,createTime,updateTime,typeId,uId,year,month,status,delStatus,sId
           FROM businessData
           WHERE id = #{id} and delStatus=1
           ORDER BY createTime DESC
       </select>
       
       <!-- 根据条件查询损益数据 -->
       <select id="listStatementBy" parameterType="map" resultType="businessData">
         SELECT s.id,s.oId,s.info,s.createTime,s.updateTime,s.typeId,s.uId,s.year,s.month,s.status,s.delStatus,s.sId,o.orgName
         FROM businessData s,organization o
         WHERE s.delStatus=1 AND s.oId=o.id
         <include refid="query_businessData_where"></include>
         ORDER BY createTime DESC
         LIMIT #{start},#{pageSize}
       </select>
       
       <!-- 根据条件修改损益数据 -->
      <update id="updateStatement" parameterType="businessData">
        update businessData
        <trim prefix="set" suffixOverrides=",">
            <if test=" oId!=null and oId!=''">oId = #{oId},</if>
            <if test="info!=null and info!=''">info = #{info},</if>
            updateTime = now(),
            <if test="typeId !=null and typeId!=''">typeId = #{typeId},</if>
            <if test="uId!=null and uId!=''">uId = #{uId},</if>
            <if test="year !=null and year!=''">year = #{year},</if>
            <if test="month!=null and month!=''">month = #{month},</if>
            <if test="status!=null and status!=''"> status = #{status}</if>
            <if test="delStatus!=null and delStatus!=''"> delStatus = #{delStatus}</if>
        </trim>
         where id = #{id} and sId=#{sId}
      </update>
      
      <!-- 删除用户  status=0 -->
      <update id="deleteStatement">
        UPDATE businessData SET delStatus=0 WHERE id=#{id}
      </update>
    
      <!--sql-->
     <sql id="query_businessData_where">
        <if test="id!=null and id!=''">
            and s.id = #{id}
        </if>
        <if test=" oId!=null and oId!=''">
            and s.oId = #{oId}
        </if>
        <if test="info!=null and info!=''">
            and s.info = #{info}
        </if>
         <if test="createTime!=null and createTime!=''">
            and s.createTime =#{createTime}
         </if>
         <if test="updateTime!=null and updateTime!=''">
            and s.updateTime = #{updateTime}
         </if>
        <if test="typeId !=null and typeId!=''">
            and s.typeId = #{typeId}
        </if>
        <if test="uId!=null and uId!=''">
            and s.uId = #{uId}
        </if>
        <if test="year !=null and year!=''">
            and s.year = #{year}
        </if>
        <if test="month!=null and month!=''">
            and s.month = #{month}
         </if>
         <if test="status!=null and status!=''">
            and s.status = #{status}
         </if>
         <if test="sId!=null and sId!=''">
            and s.sId = #{sId}
         </if>
         <if test="orgName!=null and orgName!=''">
            and o.orgName  LIKE "%"#{orgName}"%"
         </if>
    </sql>
    
</mapper>